name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.4.1
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

publish_to: none

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.5
  esc_pos_utils: ^1.1.0
  flutter_bluetooth_basic:
    git: 
      url: 'https://github.com/anhhtbk/flutter_bluetooth_basic'
      ref: master
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
